# MCP-RS Tests README

## 📋 テスト構造

MCP-RSプロジェクトのテストは以下のように構造化されています:

```
tests/
├── run-tests.ps1           # メインテストランナー
├── transport/              # Transport層テスト
│   ├── test-stdio.ps1     # STDIO Transport テスト
│   └── test-http.ps1      # HTTP Transport テスト
├── api/                   # API層テスト
│   └── test-wordpress.ps1 # WordPress API テスト
├── integration/           # 統合テスト (Rust)
├── unit/                  # 単体テスト (Rust)
└── fixtures/              # テストデータ・設定
```

## � テスト実行

### 基本的な使用方法

```powershell
# 全テストを実行
.\tests\run-tests.ps1

# 特定のテストスイートを実行
.\tests\run-tests.ps1 -TestSuite transport
.\tests\run-tests.ps1 -TestSuite api
.\tests\run-tests.ps1 -TestSuite integration

# サーバーを自動起動してテスト
.\tests\run-tests.ps1 -StartServer

# HTTPのみテスト
.\tests\run-tests.ps1 -Transport http

# 詳細出力でテスト
.\tests\run-tests.ps1 -Verbose
```

### 個別テスト実行

```powershell
# HTTP Transportテスト
.\tests\transport\test-http.ps1

# STDIO Transportテスト
.\tests\transport\test-stdio.ps1

# WordPress APIテスト
.\tests\api\test-wordpress.ps1
```

## 📊 テストスイート詳細

### Transport層テスト (`transport/`)

**HTTP Transport (`test-http.ps1`)**
- サーバー接続確認
- MCP初期化プロトコル
- リソース読み取り・一覧取得
- ツール実行・一覧取得
- エラーハンドリング

**STDIO Transport (`test-stdio.ps1`)**
- JSON-RPC 2.0フォーマット検証
- Content-Lengthヘッダー形式
- MCP通信プロトコル準拠性

### API層テスト (`api/`)

**WordPress API (`test-wordpress.ps1`)**
- カテゴリ取得・構造検証
- 投稿取得・フィールド検証
- ツールリスト・可用性確認
- 検索機能・パフォーマンス
- データ整合性チェック

## ⚙️ 設定とオプション

### 環境設定

テスト実行前に以下を確認してください:

```powershell
# MCP-RSをビルド
cargo build

# 設定ファイルが存在することを確認
# - mcp-config-webui.toml (HTTP用)
# - mcp-config.toml (STDIO用)
```

### カスタマイズオプション

| パラメータ | デフォルト | 説明 |
|----------|----------|------|
| `TestSuite` | `all` | 実行するテストスイート |
| `Transport` | `http` | テスト対象Transport |
| `ServerUrl` | `http://127.0.0.1:8081/mcp` | サーバーURL |
| `ConfigPath` | `mcp-config-webui.toml` | 設定ファイル |
| `StartServer` | `false` | 自動サーバー起動 |
| `TimeoutSeconds` | `30` | タイムアウト時間 |

## 🔧 トラブルシューティング

### よくある問題

**1. サーバー接続エラー**
```powershell
# サーバーが起動していることを確認
Get-Process -Name "mcp-rs"

# 手動でサーバー起動
.\target\debug\mcp-rs.exe --config mcp-config-webui.toml
```

**2. ポート競合**
```powershell
# ポート使用状況を確認
netstat -ano | findstr :8081

# 設定ファイルでポートを変更
# mcp-config-webui.toml の port = 8081 を変更
```

**3. WordPress API エラー**
```powershell
# WordPress設定を確認
# mcp-config-webui.toml の [wordpress] セクション
```

### ログとデバッグ

```powershell
# 詳細ログでテスト実行
.\tests\run-tests.ps1 -Verbose

# サーバーログを確認
# logs/mcp-rs.log または標準出力
```

## � 継続的インテグレーション

### GitHub Actions統合例

```yaml
name: MCP-RS Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build MCP-RS
        run: cargo build
      - name: Run Tests
        run: .\tests\run-tests.ps1 -StartServer
```

### ローカル開発ワークフロー

```powershell
# 1. 開発後のテスト実行
cargo build && .\tests\run-tests.ps1 -StartServer

# 2. 特定機能のテスト
.\tests\api\test-wordpress.ps1

# 3. コミット前の完全テスト
.\tests\run-tests.ps1 -TestSuite all -Verbose
```

## 🔄 テストの追加・拡張

### 新しいAPIテストの追加

1. `tests/api/` に新しいテストファイルを作成
2. `Invoke-MCPRequest` 関数を使用してMCP通信
3. `Write-TestResult` 関数で結果レポート

### Transportテストの拡張

1. `tests/transport/` でプロトコル仕様に沿ったテスト
2. エラーケース・エッジケースの追加
3. パフォーマンステストの実装

## 📝 テスト作成ガイドライン

- **明確な目的**: 各テストが何を検証するかを明確に
- **独立性**: テスト間の依存関係を避ける
- **再現性**: 同じ環境で同じ結果が得られる
- **包括性**: 正常系・異常系の両方をカバー
- **保守性**: コードの変更に対して適切に更新される
├── README.md                    # このファイル
├── mod.rs                       # モジュール定義
├── unit/                        # 単体テスト
│   ├── mod.rs
│   ├── config_tests.rs         # 設定システム
│   ├── handler_tests.rs        # ハンドラーレジストリ
│   ├── runtime_tests.rs        # ランタイムシステム
│   └── transport_tests.rs      # 通信レイヤー
├── integration/                 # 統合テスト
│   ├── mod.rs
│   ├── session_management.rs   # セッション管理（移行予定）
│   ├── plugin_system.rs        # プラグインシステム（移行予定）
│   ├── policy_management.rs    # ポリシー管理（移行予定）
│   └── rollback_system.rs      # ロールバックシステム（移行予定）
├── database/                    # データベース関連
│   ├── mod.rs
│   ├── engine_tests.rs         # データベースエンジン（移行予定）
│   ├── handler_tests.rs        # データベースハンドラー（移行予定）
│   └── security_tests.rs       # データベースセキュリティ（移行予定）
├── wordpress/                   # WordPress統合
│   ├── mod.rs
│   ├── api_tests.rs            # WordPress API（移行予定）
│   ├── content_tests.rs        # コンテンツ管理（移行予定）
│   ├── media_tests.rs          # メディア管理（移行予定）
│   └── security_tests.rs       # WordPress セキュリティ（移行予定）
├── canary/                      # カナリーデプロイ
│   ├── mod.rs
│   └── deployment_tests.rs     # デプロイメント（移行予定）
├── performance/                 # パフォーマンス
│   ├── mod.rs
│   ├── database_benchmarks.rs  # データベースベンチマーク ✅
│   └── memory_benchmarks.rs    # メモリベンチマーク（予定）
├── fixtures/                    # テストデータ
│   ├── mod.rs
│   ├── test_data.rs           # テストデータ（予定）
│   └── mock_services.rs       # モックサービス（予定）
└── [legacy files]              # 既存ファイル（段階的移行中）
    ├── session_current_tests.rs
    ├── rollback_tests.rs
    ├── integration_tests.rs
    ├── plugin_integration_tests.rs
    ├── plugin_system_integration_tests.rs
    ├── policy_hot_reload_integration.rs
    ├── basic_session_tests.rs
    └── simple_tests.rs          # ➡️ unit/ に分割済み
```

## 🧪 テスト実行方法

### 全テスト実行
```bash
cargo test
```

### カテゴリ別テスト実行
```bash
# 単体テスト
cargo test unit::

# 統合テスト
cargo test integration::

# データベーステスト
cargo test database::

# WordPressテスト
cargo test wordpress::

# パフォーマンステスト（リリースモード推奨）
cargo test performance:: --release

# カナリーデプロイテスト
cargo test canary::
```

### 個別テストファイル実行
```bash
# 設定テスト
cargo test unit::config_tests

# ハンドラーテスト
cargo test unit::handler_tests

# データベースベンチマーク
cargo test performance::database_benchmarks --release
```

## 📊 現在のテスト状況

### ✅ 移行完了
- **単体テスト**: config_tests, handler_tests, runtime_tests, transport_tests
- **パフォーマンステスト**: database_benchmarks

### 🚧 移行予定
- **統合テスト**: セッション管理、プラグインシステム、ポリシー管理、ロールバック
- **データベーステスト**: エンジン、ハンドラー、セキュリティ
- **WordPressテスト**: API、コンテンツ、メディア、セキュリティ
- **カナリーデプロイテスト**: デプロイメント機能

### 📈 テスト実行結果
- **ライブラリテスト**: 131/131 (130 passed, 1 ignored)
- **統合テスト**: 4/4 passed
- **実行時間**: ~0.20s (lib), ~0.00s (integration)

## 🔄 移行計画

### Phase 1: 基本構造 ✅
- [x] 新しいディレクトリ構造作成
- [x] モジュール定義ファイル作成
- [x] 単体テストの分割と移行

### Phase 2: テストファイル移行（進行中）
- [ ] 統合テストファイルの整理と移行
- [ ] データベーステストの統合
- [ ] WordPressテストの整理

### Phase 3: 重複除去と最適化
- [ ] 重複テストの削除
- [ ] テスト実行時間の最適化
- [ ] CI/CDとの統合

### Phase 4: 完了と文書化
- [ ] レガシーファイルの削除
- [ ] テストガイドの完成
- [ ] ベストプラクティスの確立

## 🎯 設計原則

1. **明確な分離**: 機能別・レイヤー別の明確な分類
2. **実行効率**: カテゴリ別実行による高速化
3. **保守性**: 一貫した命名規則と構造
4. **拡張性**: 新機能テストの追加しやすさ
5. **CI/CD対応**: 段階的テスト実行による最適化

---

**次の目標**: 統合テストとデータベーステストの移行完了
