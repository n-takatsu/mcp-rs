# Generated by Cargo
/target/
# Cargo.lock  # Note: Keeping Cargo.lock tracked for binary reproducibility

# Configuration files with sensitive information
mcp-config.toml

# Test-generated certificates and keys (contains sensitive data)
/certs/

# IDE files (但し.vscode/はプロジェクト設定として含める)
# MCP設定はアクセストークンが含まれる可能性があるため除外
.vscode/mcp.json
.idea/

# Claude Code: settings.json はセッションごとに承認したコマンドの許可リストが
# 自動追記される個人利用向けファイルであり、チーム共有設定ではないため除外する。
# settings.local.json・scheduled_tasks.lock も同様に個人用/実行時ファイル。
.claude/settings.json
.claude/settings.local.json
.claude/scheduled_tasks.lock
*.swp
*.swo
*~
*.bak

# OS files
.DS_Store
Thumbs.db

# Log files
*.log
# src/logging.rs writes daily-rotated files here as mcp-rs.log.<date>,
# which *.log alone doesn't match
/logs/

# CI/CD generated files
clippy-report.json

# Environment files
.env
.env.local
.env.production
.env.wordpress.local

# Release directories (should use GitHub Releases instead)
/mcp-rs-v*/
mcp-rs-v*-release/

# Release artifacts (binaries, zips)
*.zip
*.exe
/release-artifacts/

# Report files (documentation should be in wiki or separate docs repo)
*_REPORT.md
*_TEST_REPORT.md
MYSQL_SECURITY_TEST_REPORT.md
/reports/

# Release management
# Keep versioned release folders but ignore temporary build artifacts
mcp-rs-v*-release/*.zip
mcp-rs-v*-release/temp/
# Note: mcp-rs-v*-release/ folders themselves are tracked for version history
